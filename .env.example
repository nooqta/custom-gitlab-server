# GitLab Personal Access Token (required)
# Generate one from your GitLab profile: Settings -> Access Tokens
# Required scopes: api, read_api
GITLAB_PERSONAL_ACCESS_TOKEN=YOUR_GITLAB_TOKEN_HERE

# GitLab API URL (optional, defaults to https://gitlab.com/api/v4)
# Use this if you are connecting to a self-hosted GitLab instance
# Example: GITLAB_API_URL=https://gitlab.yourcompany.com/api/v4
GITLAB_API_URL=https://gitlab.com/api/v4
